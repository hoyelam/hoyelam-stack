interface:
  display_name: "Prove The Work"
  short_description: "Clarify acceptance and assess verification evidence"
  default_prompt: "Use $prove-the-work to resolve unclear acceptance checks or assess the evidence for this change."
policy:
  allow_implicit_invocation: true
