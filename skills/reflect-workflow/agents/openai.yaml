interface:
  display_name: "Reflect Workflow"
  short_description: "Capture repeated lessons without bloating rules"
  default_prompt: "Use $reflect-workflow to identify evidence-backed improvements from this completed work."
policy:
  allow_implicit_invocation: true
