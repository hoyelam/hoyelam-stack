interface:
  display_name: "Investigate First"
  short_description: "Find the real cause and owning code first"
  default_prompt: "Use $investigate-first to locate and explain the root cause before proposing code."
policy:
  allow_implicit_invocation: true
