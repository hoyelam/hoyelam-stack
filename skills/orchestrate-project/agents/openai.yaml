interface:
  display_name: "Orchestrate Project"
  short_description: "Coordinate durable multi-unit engineering programs"
  default_prompt: "Use $orchestrate-project to coordinate this multi-unit project through verified completion."
policy:
  allow_implicit_invocation: true
