interface:
  display_name: "Hoyelam Mode"
  short_description: "Engineering defaults and verified completion"
  default_prompt: "Use $hoyelam-mode to complete this task with appropriate tools, scope, and verification."
policy:
  allow_implicit_invocation: true
