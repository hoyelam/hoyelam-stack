interface:
  display_name: "Hoyelam Mode"
  short_description: "Orchestrate useful work and verify outcomes"
  default_prompt: "Use $hoyelam-mode to choose useful tools, delegate independent work, and verify the outcome."
policy:
  allow_implicit_invocation: true
