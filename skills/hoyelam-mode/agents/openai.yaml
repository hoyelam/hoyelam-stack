interface:
  display_name: "Hoyelam Mode"
  short_description: "Complete root-cause-first engineering work"
  default_prompt: "Use $hoyelam-mode to inspect what is available and select a proportionate workflow for this task."
policy:
  allow_implicit_invocation: true
