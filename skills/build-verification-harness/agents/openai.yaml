interface:
  display_name: "Build Verification Harness"
  short_description: "Make real behavior repeatably verifiable"
  default_prompt: "Use $build-verification-harness to create and prove a project-local real-app verification path."
policy:
  allow_implicit_invocation: true
