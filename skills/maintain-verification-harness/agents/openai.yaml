interface:
  display_name: "Maintain Verification Harness"
  short_description: "Repair harness drift and verify affected behavior"
  default_prompt: "Use $maintain-verification-harness to repair this project's affected verification paths and prove the selected coverage."
policy:
  allow_implicit_invocation: true
