# Skills and global-instruction hardening — 2026-09-06

The confirmed audit findings were resolved locally. Verification still governs completion, and orchestration retains scoped briefs, ownership, review, and verified integration. Generic always-on instructions were reduced; detailed tool guidance is loaded when relevant.

## Changes

- Global Codex and Claude instructions require current acceptance evidence, preserve existing implementation authority, and distinguish a clean review from completed work. Claude retains its preference for coordinating Codex implementation.
- Repository instructions point to the maintained workflow. Verification planning remains before implementation, with detailed guidance loaded when needed.
- Local and third-party skills now narrow unrelated triggers, avoid repeated approval and setup gates, preserve existing project architecture and equivalent tooling, and require observed evidence rather than assumed success. Confirmed review findings in the staged fixes were corrected and affected checks rerun.
- The installed edit manifest contains 129 files, including identical copies and supporting references/tests. Replaced originals and exact after hashes are preserved in the private override bundle.
- Thirty-two identical duplicate skill trees were moved out of Codex discovery while their copies in `~/.agents/skills` remain. All four self-improvement copies in Codex/Claude were confirmed OpenClaw-only and moved out of those hosts' discovery; the earlier assumption that a supported copy remained was incorrect. Their directories are preserved intact.
- All 16 workflow source links resolve to this checkout, including the two new iOS/macOS entrypoints. The cached duplicate hoyelam plugin is disabled; unrelated configuration is preserved.

## Evidence

Staged validation checked 96 skill entrypoints, 305 relative links, all original hashes, and the exact configuration delta. Installed verification checked 129 file hashes, 16 source links, all 36 quarantined paths, and unchanged unrelated configuration.

The isolated quota repair reproduced a deliberately supplied valid review finding. The final four-test suite passes and fails against the original source. Independent review also checked 275 partition cases. The required packaging checker remained unchanged and exited 78 because its fixture signing service was unavailable; the worker explicitly withheld a readiness claim. Planning records exist, but full worker tool-ordering was not independently observed.

The repository validator, skill metadata validator, and plugin validator are the structural checks for these repository changes. Their final command results are recorded in the task handoff and override bundle; structural checks alone do not establish agent reliability.

## Local maintenance

The private backup and override manifest is at `/Users/hoyelam/.local/share/hoyelam/skill-overrides/2026-09-06-verification`. It contains originals, quarantined trees, audit findings, review/validation records, and the isolated exercise. Agent sessions need refreshing to load the new discovery state. Third-party updates may overwrite local overrides; compare before/after hashes and review changed upstream instructions before reapplying.

This was a targeted resolution of confirmed findings, not a claim that every audited skill or embedded platform example is defect-free. No real Apple/Electron app or external release flow was exercised. Historical workflow evidence remains tied to its original tested state.
